FROM python:3.12-slim

WORKDIR /app

# runtime deps
RUN pip install --no-cache-dir fastapi uvicorn pydantic

# copy only the handrail package
COPY handrail ./handrail

EXPOSE 8011

CMD ["uvicorn", "handrail.server:app", "--host", "0.0.0.0", "--port", "8011"]
