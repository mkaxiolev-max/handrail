\* Apalache bounded model check — I5_RiskTierGate
\* NS∞ Dignity Kernel  |  AXIOLEV Holdings LLC © 2026
\*
\* Property: HIGH_RISK_OPS ∩ executed ⊆ secure_executed — R3/R4 ops only
\*           enter executed when yubikey_ok=TRUE at the time of the call
\*           (tracked via ghost set secure_executed).
\* Domain cardinality: 4 (AUTH_BYPASS=0, SYS_DESTRUCT=1, POLICY_OVERRIDE=2, H_OP=3)
\* Constants are initialised via --cinit=CInit in the TLA+ module.
\*
\* Run:
\*   apalache-mc check --length=8 --inv=I5_RiskTierGate --cinit=CInit Dignity.tla

CONSTANTS
    AUTH_BYPASS     = 0
    SYS_DESTRUCT    = 1
    POLICY_OVERRIDE = 2
    H_OP            = 3

INIT Init
NEXT Next
INVARIANT I5_RiskTierGate
