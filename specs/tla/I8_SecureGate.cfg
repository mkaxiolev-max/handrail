\* Apalache bounded model check — I8_SecureGate
\* NS∞ Dignity Kernel  |  AXIOLEV Holdings LLC © 2026
\*
\* Property: secure_executed ⊆ executed ∩ HIGH_RISK_OPS — ghost-set consistency;
\*           secure_executed is a sound under-approximation: it contains only ops
\*           that are both in executed AND in HIGH_RISK_OPS (YubiKey gate cleared).
\* Domain cardinality: 4 (AUTH_BYPASS=0, SYS_DESTRUCT=1, POLICY_OVERRIDE=2, H_OP=3)
\* Constants are initialised via --cinit=CInit in the TLA+ module.
\*
\* Run:
\*   apalache-mc check --length=8 --inv=I8_SecureGate --cinit=CInit Dignity.tla

CONSTANTS
    AUTH_BYPASS     = 0
    SYS_DESTRUCT    = 1
    POLICY_OVERRIDE = 2
    H_OP            = 3

INIT Init
NEXT Next
INVARIANT I8_SecureGate
