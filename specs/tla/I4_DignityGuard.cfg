\* Apalache bounded model check — I4_DignityGuard
\* NS∞ Dignity Kernel  |  AXIOLEV Holdings LLC © 2026
\*
\* Property: ABSOLUTE_NEVER ∩ executed = {} — all unconditional never-events
\*           (auth.bypass, sys.self_destruct) are excluded from executed in every state.
\* Domain cardinality: 4 (AUTH_BYPASS=0, SYS_DESTRUCT=1, POLICY_OVERRIDE=2, H_OP=3)
\* Constants are initialised via --cinit=CInit in the TLA+ module.
\*
\* Run:
\*   apalache-mc check --length=8 --inv=I4_DignityGuard --cinit=CInit Dignity.tla

CONSTANTS
    AUTH_BYPASS     = 0
    SYS_DESTRUCT    = 1
    POLICY_OVERRIDE = 2
    H_OP            = 3

INIT Init
NEXT Next
INVARIANT I4_DignityGuard
