\* Apalache bounded model check — I3_PolicyQuorum
\* NS∞ Dignity Kernel  |  AXIOLEV Holdings LLC © 2026
\*
\* Property: policy.override (POLICY_OVERRIDE) only in executed if quorum_count >= 2
\*           at time of execution (tracked via ghost set quorum_executed).
\* Domain cardinality: 4 (AUTH_BYPASS=0, SYS_DESTRUCT=1, POLICY_OVERRIDE=2, H_OP=3)
\* Constants are initialised via --cinit=CInit in the TLA+ module.
\*
\* Run:
\*   apalache-mc check --length=8 --inv=I3_PolicyQuorum --cinit=CInit Dignity.tla

CONSTANTS
    AUTH_BYPASS     = 0
    SYS_DESTRUCT    = 1
    POLICY_OVERRIDE = 2
    H_OP            = 3

INIT Init
NEXT Next
INVARIANT I3_PolicyQuorum
