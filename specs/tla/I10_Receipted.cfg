\* Apalache bounded model check — I10_Receipted
\* NS∞ Dignity Kernel  |  AXIOLEV Holdings LLC © 2026
\*
\* Property: ∀ op ∈ executed: ∃ i ∈ DOMAIN receipts: receipts[i].op = op ∧ outcome="executed"
\*           Every executed kernel decision is receipted — no silent executions.
\*           Maps to /Volumes/NSExternal/ALEXANDRIA/ledger/kernel_decisions.jsonl.
\* Domain cardinality: 4 (AUTH_BYPASS=0, SYS_DESTRUCT=1, POLICY_OVERRIDE=2, H_OP=3)
\* Constants are initialised via --cinit=CInit in the TLA+ module.
\*
\* Run:
\*   apalache-mc check --length=8 --inv=I10_Receipted --cinit=CInit Dignity.tla

CONSTANTS
    AUTH_BYPASS     = 0
    SYS_DESTRUCT    = 1
    POLICY_OVERRIDE = 2
    H_OP            = 3

INIT Init
NEXT Next
INVARIANT I10_Receipted
